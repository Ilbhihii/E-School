<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting up -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:statusBarColor">#080c14</item>
        <item name="android:navigationBarColor">#080c14</item>
    </style>

    <!-- Theme applied to the Android Window as soon as Flutter renders -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:statusBarColor">#080c14</item>
        <item name="android:navigationBarColor">#080c14</item>
    </style>
</resources>
